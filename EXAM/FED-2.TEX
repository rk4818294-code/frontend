%  END  SEM-2

% for 2 marks questions 
% do syntax well   
% correct syntax of media query- @media


%  frontend important questions:
%  10m:-

%  create bootstrap grid which have 2 responsive carts that appear side by side on large screen and stack vertically on small screen?          
%  create responsive webpage using grid that display 3 cards in a row on a large screen and 1 card per row on  small screen?                   
%  construct a git command to clone a repository ,and explain its purpose                                                                      
%  Command to clone a repo, create a new file, commit the changes, push them to repo.
%  explain git commands to merge one branch into another branch and explain the purpose of merge operation?
%  Git command to create a branch, switch the branch, merge into main, push to GitHub, and explain the concept of branching.

%  6m

%  Build HTML code to create a responsive card with image, title, and description with an example.                                            
%  Write a Bootstrap code to create a navbar and link Home, Contact, Service.                                                                                                   
%  Explain the purpose of git ignore file with an example.
%  write steps to clone a repository from GitHub and list repository files?

--------------------------------------------------------------------------------------------------------------------------------------------------


% unit-4     10 marks
% q1- a react component that except props and display dynamic data based on its state?
% q2- a component that incorporate inline styling and rendor content based on user interaction?
% q3- a component that manages both state and props and uses useeffect hook s to handle side effects?
% q4- make use of  usestate and useeffect hook to display current time?
% q5- a component that except props and change state based on user input?
% q6- light and dark mode
% q7- create a structure with multiple components communicating by props and using usestate for data management?
% q8- a react component that dynamically change styles based on props?
% q9- a reusable button with css modules?
% q10- light and dark encounter application?


% unit-5              10 marks

% q1- a multi page application with route specific layout?
% q2- a navigation bar that highlight active route using custom style?
% q3- implement nested routes and lazy loading?

% backend logic question
% q1- deploy api or indication method to securely access protected api authentication?
% q2- structure an api request using proper end points and json format?

% unit 4 and 5
% 6 marks
% q1-useeffect hook to update document title whenever state value changes?
% q2- an application that uses both inline style and css modules for a component?
% q3- write a dynamic route for product details page?
% q4- a navigation that highlight active link using nav link?
 

